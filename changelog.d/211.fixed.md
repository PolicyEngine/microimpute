The wheel now declares its subpackages properly instead of sweeping the source tree in as package data, so stale `__pycache__` and other working-tree files no longer ship. Adds `py.typed`, the full author list, classifiers and project URLs.
